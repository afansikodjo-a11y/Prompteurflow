/**
 * API publique de la feature « pwa » (installation sur l'appareil).
 */
export { InstallButton } from "./components/install-button";
export { useInstallPrompt, type UseInstallPromptResult } from "./hooks/use-install-prompt";
