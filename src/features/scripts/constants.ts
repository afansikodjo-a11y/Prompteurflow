/** Clé localStorage de la collection de scripts. */
export const SCRIPTS_STORAGE_KEY = "prompteurflow:scripts";

/** Clé localStorage de l'identifiant du script sélectionné. */
export const CURRENT_SCRIPT_STORAGE_KEY = "prompteurflow:current-script";

/** Titre par défaut d'un script sans nom. */
export const UNTITLED_SCRIPT = "Sans titre";
